var width = prompt("Enter width please:");
var height = prompt("Enter height:");
var testWidth = 400;
var testHeight = 500;
var counter;
var myArray = [];


function convert(userW, userH, testW, testH) {
	var counter;
	var reshape;
	for (counter = 0; counter <= 4; counter++) {
		
		reshape = myArray[counter]
	}
}