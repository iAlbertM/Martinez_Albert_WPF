// Albert Martinez
// web programming fundamentals
// Functions - Returning Values

var total = calcArea(30, 20); // whatever the function returns, store into this variable total

function calcArea(w, h) { // storage bin the info gets stored in w=30, h=20
	var area = w * h;
	return area; // spitting the info out
}

console.log(total);

// functions that are returning information must be stored into a variable
